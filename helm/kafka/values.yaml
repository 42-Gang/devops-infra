clusterId: "kraft-cluster-id-01"

controller:
  replicaCount: 1
  controllerOnly: true
  quorumBootstrapServers: "0@kafka-controller-0.kafka-controller-headless.msa.svc.cluster.local:9093"
  persistence:
    enabled: true
    storageClass: local-path
    size: 1Gi
  resources:
    requests:
      cpu: "300m"
      memory: "1Gi"
    limits:
      cpu: "500m"
      memory: "2Gi"
  tolerations:
    - key: "node-role.kubernetes.io/control-plane"
      operator: "Exists"
      effect: "NoSchedule"

broker:
  replicaCount: 1
  persistence:
    enabled: true
    storageClass: local-path
    size: 1Gi
  resources:
    requests:
      cpu: "300m"
      memory: "1Gi"
    limits:
      cpu: "500m"
      memory: "2Gi"
  tolerations:
    - key: "node-role.kubernetes.io/control-plane"
      operator: "Exists"
      effect: "NoSchedule"

extraEnvVars:
  - name: ALLOW_PLAINTEXT_LISTENER
    value: "yes"

listeners:
  client:
    protocol: PLAINTEXT
    containerPort: 9092
    name: CLIENT
  interbroker:
    protocol: PLAINTEXT
    containerPort: 9094
    name: INTERNAL
  controller:
    protocol: PLAINTEXT
    containerPort: 9093
    name: CONTROLLER
