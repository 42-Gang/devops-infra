apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Chart.Name }}
  labels:
    app: {{ .Chart.Name }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      app: {{ .Chart.Name }}
  template:
    metadata:
      labels:
        app: {{ .Chart.Name }}
    spec:
      tolerations:
        - key: "node-role.kubernetes.io/control-plane"
          operator: Exists
          effect: NoSchedule
      containers:
        - name: {{ .Chart.Name }}
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - containerPort: {{ .Values.service.port }}
          env:
            - name: DB_URL
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.env.SECRET_NAME }}
                  key: MAIN_GAME_DB_URL
            - name: FASTIFY_LOG_LEVEL
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.env.SECRET_NAME }}
                  key: FASTIFY_LOG_LEVEL
            - name: REDIS_HOST
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.env.SECRET_NAME }}
                  key: MAIN_GAME_REDIS_HOST
            - name: REDIS_PORT
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.env.SECRET_NAME }}
                  key: MAIN_GAME_REDIS_PORT
            - name: KAFKA_BROKER
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.env.SECRET_NAME }}
                  key: KAFKA_BROKER
            - name: USER_SERVER_URL
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.env.SECRET_NAME }}
                  key: USER_SERVER_URL
            - name: AUTH_SERVER_URL
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.env.SECRET_NAME }}
                  key: AUTH_SERVER_URL
            - name: GMAIL_USER
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.env.SECRET_NAME }}
                  key: GMAIL_USER
            - name: GOOGLE_CLIENT_ID
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.env.SECRET_NAME }}
                  key: GOOGLE_CLIENT_ID
            - name: GOOGLE_CLIENT_SECRET
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.env.SECRET_NAME }}
                  key: GOOGLE_CLIENT_SECRET
            - name: GOOGLE_REFRESH_TOKEN
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.env.SECRET_NAME }}
                  key: GOOGLE_REFRESH_TOKEN
            - name: FASTIFY_PORT
              value: "{{ .Values.service.port }}"
