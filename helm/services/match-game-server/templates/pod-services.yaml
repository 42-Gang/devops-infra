{{- $name := .Chart.Name -}}
{{- $port := .Values.service.port -}}
{{- $replicas := int .Values.replicaCount -}}
{{- range $i := until $replicas }}
apiVersion: v1
kind: Service
metadata:
  name: {{ printf "%s-%d" $name $i }}
  labels:
    app: {{ $name }}
    podOrdinal: "{{ $i }}"
spec:
  selector:
    statefulset.kubernetes.io/pod-name: {{ printf "%s-%d" $name $i }}
  ports:
    - name: game
      port: {{ $port }}
      targetPort: {{ $port }}

---
{{- end }}
