dbUser:
  existingSecret:
    name: cassandra-admin-auth
    namespace: monitor


resourcesPreset: "none"

resources:
  requests:
    cpu: "250m"
    memory: "512Mi"
  limits:
    cpu: "500m"
    memory: "2Gi"

persistence:
  storageClass: local-path
  size: 6Gi

jvm:
  maxHeapSize: "1024m"