apiVersion: traefik.io/v1alpha1
kind: Middleware
metadata:
  name: ws-middleware
  namespace: msa
spec:
  replacePathRegex:
    regex: ^/ws/(?:user|chat|main-game)/(.*)
    replacement: /socket.io/${1}
